defmodule ReactiveRegistryTest do
  use ExUnit.Case

  # test "do" do
  #   {:ok, pid} = Reactive.Registry.init()
  #   assert pid |> is_pid() |> dbg()
  #   Reactive.Registry.supervisor() |> is_pid() |> dbg()
  #   Reactive.Registry.supervisor() |> is_pid() |> dbg()
  # end
end
